server:
  port: 20000
spring:
  redis:
    enable: true
    host: 192.168.125.15
    database: 10
    password: jiaozi
    timeout: 6000ms
    jedis:
      pool:
        max-active: 1000  # 连接池最大连接数（使用负值表示没有限制）
        max-wait: -1ms      # 连接池最大阻塞等待时间（使用负值表示没有限制）
        max-idle: 10      # 连接池中的最大空闲连接
        min-idle: 5       # 连接池中的最小空闲连接
xuper:
  client: 192.168.125.15:37101
  account-mnemonic: mechanic fragile breeze enhance velvet inform test hawk child fault scrub call